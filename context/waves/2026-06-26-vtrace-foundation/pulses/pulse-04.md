# Pulse 04: SPECIFICATION_BASELINE

Settled. Authored `docs/vtrace/SPECIFICATION_BASELINE.md` (target/provisional): the **scale
model** (SCALE-01..03), the maritime dimension pool `DIM-01..13`, the demand basis (SPEC-DB-01/02:
surge/average + tide-restricted/all-tide), the **T1–T4 tier model** (International Gateway Port →
National Port → Regional/Inland Port → Local Terminal/Berth) with throughput/dwell/connectivity/
access SLA terms, controlled specs `SPEC-001..013`, public contracts `IF-001..004`, and the spec
gate (`pass_with_risk`).

Role-review fixed point (real `.roles`): one finding applied — throughput (DIM-01) risked reading
as control-free, resolved by keeping alliance diversity (DIM-07) in SPEC-007 + the access SLA
term. Pool weights, SLA thresholds, and scale nesting (DEF-005) explicitly provisional. Next:
TRACE.
